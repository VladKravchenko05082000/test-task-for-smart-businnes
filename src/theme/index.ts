import { colors } from "./colors";
import {
  fontWeight,
  fonts,
  mediaQueries,
  breakpoints,
  siteWidth,
} from "./base";

export const theme = {
  colors,
  siteWidth,
  mediaQueries,
  fontWeight,
  fonts,
  breakpoints,
};
